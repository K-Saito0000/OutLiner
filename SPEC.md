# OutLiner 機能仕様書

最終更新: 2026-07-24

## 1. 概要

OutLiner は、画像からオブジェクトを切り出してレーザーカッター（カットパスSVG）・UVプリンター（白版PNG）の加工データを作成するWebアプリケーション。入力は2タイプ:

- **透過PNGモード**: アルファチャンネル付きPNGの透明部分を検出して切り出す
- **写真モード**: 写真・画像からAI（u2netp）で被写体を切り抜き、任意で減色してから切り出す

### 動作要件・構成

| 項目 | 内容 |
|---|---|
| 構成 | `outliner.html`（vanilla JS + Canvas API）＋ `vendor/`（AIモデル類、写真モード使用時のみfetch）。CDN不使用 |
| 処理場所 | すべてブラウザ内で完結。画像は外部送信されない |
| 対応入力 | 透過PNGモード: PNG のみ／写真モード: PNG・JPEG・WebP。いずれも1枚ずつ処理 |
| 配信 | GitHub Pages（`index.html` → `outliner.html` リダイレクト）またはローカルサーバー。透過PNGモードは `file://` でも動作するが、**写真モードは vendor/ をfetchするため http(s):// 必須** |

## 2. 処理パイプライン

```
【透過PNGモード】
PNG読み込み（D&D / ファイル選択）
  → ImageData化
  ─────────────────────────────┐
【写真モード】                    │
写真読み込み → 作業解像度に縮小     │
  → AI切り抜き（u2netp）→ マスク後処理│
  → 閾値調整・ブラシ手修正          │
  → 減色（任意）→ 確定でImageData化 ┘
  → オブジェクト検出（連結成分ラベリング）
  → プレビュー表示（左: 元画像＋検出オーバーレイ / 右: 切り出し結果のレイヤー合成）
  → 切り出し・書き出し（カラーPNG / 白版PNG / 輪郭SVG）
```

## 2.5 写真モード（AI切り抜き＋減色）

「ペット刺繍メーカー」の実装を移植したもの。

### AI切り抜き
- **モデル**: u2netp（ONNX, 4.4MB）を ONNX Runtime Web 1.14（WASMバックエンド・SIMD・1スレッド）で実行
- **読み込み**: 写真モードで最初の画像を読み込んだときに `vendor/` から動的ロード（約15MB・初回のみ、以後ブラウザキャッシュ）
- **作業解像度**: 長辺1000pxに縮小した `baseCanvas`
- **推論**: 320×320にストレッチ → ImageNet正規化（mean/std）→ 推論 → 出力をmin-max正規化した**確率マップ**として保持
- **マスク生成**: 閾値2値化 → クローズ（膨張→収縮）→ 最大連結成分のみ → 穴埋め → ボックスブラー平滑 → バイリニア拡大 → 0.5閾値で作業解像度の2値マスク
- **マスク閾値**: 0.05〜0.95（既定0.45）。確率マップから再2値化するだけなので再推論なし。変更するとブラシ修正はリセット（undoで復帰可）

### ブラシ手修正
- ツール: なし／けす（背景化）／たす（前景化）、ブラシサイズ 6〜80px（表示px基準）
- 塗りは**なぞり終わり（pointerup）で反映**。なぞり中は半透明の円（けす=赤／たす=緑）でストロークを表示
- **にた色モード**（既定ON）: ストローク下の対象画素をシードに、Lab色距離しきい値20・距離上限（320グリッド比30相当）の4近傍BFSで**類似色の連結領域へ自動成長**させてまとめて切り替える（ペット刺繍メーカーの「にた色モード」と同方式）。OFFでなぞった円内のみ反映
- undo/redo: ストローク反映時・閾値変更時にスナップショット保存（上限20）
- 「AI切り抜きをやり直す」で確率マップからマスクを再生成（ブラシ修正を破棄）

### 減色ステージ（独立プロセス）
- 切り抜きステージの「減色へ進む」で遷移。減色プレビューを見ながら調整できる
- **アルゴリズム**: CIELAB空間の k-means（最遠点初期化・固定14イテレーション）。距離は明度差を2.4倍重み付け
- **色数**: 減色しない／2〜8色（既定4色）。変更すると再計算（色差し替え・まとめはリセット）
- **減色のスキップ**: 切り抜きステージの「減色をスキップして切り出しへ」で、減色ステージを経ずに切り出しへ直行できる（色数は「減色しない」になる）。スキップ後も「← 減色へ戻る」で減色ステージに入れる
- 「減色しない」を選んだ場合もパレットは持たないだけで、範囲指定による色調整（原画・質感・単色エリア）は使用できる
- **対象**: alpha>0 の画素のみ。サンプリング上限1.5万点でパレット決定後、全画素を最近傍パレット色に割当（画素→スロットのマップを保持）
- **範囲を指定して色を調整**: 減色の例外にしたい範囲・独立させたい範囲を指定できる

  **範囲の指定方法**
  - **クリック**: クリック点3×3をシードに、しきい値15・距離上限つきBFSで似た色の範囲を自動選択（ピンポイント指定向け。ペット刺繍メーカーの addRegionAt 方式）
  - **ブラシ**: なぞった範囲（サイズ6〜120px）を選択。広い範囲や複数箇所をまとめて指定できる。「にた色まで広げる」ONでなぞった色に似た周囲まで自動成長、OFFでなぞった範囲ちょうど

  **指定した範囲の色**
  - **元画像のまま**（既定）: その範囲は減色せず、**元画像の色をそのまま再現**する（顔まわりだけフルカラーで残す、など）。パレットには領域サムネイル付きの「原画エリア」として並ぶ。代表色を持たないため色差し替え・まとめの対象外
  - **この範囲だけ細かく減色**: その範囲だけ**多めの色数（6/8/12/16/24/32色、既定12色）で個別にk-means減色**する。1色にまとめず画素ごとに元画像へ近い色が残るため、毛並みや陰影といった質感を保ったまま色を簡略化できる。パレットには領域サムネイル付きで並び、代表色1色を持たないため色差し替え・まとめの対象外
  - **単色にする**: その範囲を代表色1色でベタ塗り。色差し替え・まとめが可能

  - 共通: 代表色はシード色優先ブレンド（L: 65/35, a・b: 75/25）。背景のみの指定は拒否、最小面積未満は失敗メッセージ。「けす」で個別に取り消し可能
- **色のまとめ**: パレット行をクリックして2色以上選ぶと「どの色にまとめるか」の選択UIが出現。まとめた色は1スロットとして扱われ（子→親のマージマップ・連鎖解決）、「まとめをぜんぶもどす」で一括解除
- **色の差し替え**: パレット各行のスウォッチ（カラーピッカー）でスロット色を変更（「変更済」バッジ・構成比%表示）
- 色数変更時は差し替え・まとめをリセット（クリック追加色は保持）。「色調整をリセット」で差し替え・まとめ・追加色をすべて初期化

### ステップ間ナビゲーション（往復可能）
```
切り抜き ⇄ 減色 ⇄ 切り出し（メイン）
```
- 切り抜き→「減色へ進む」→ 減色→「この内容で切り出しへ進む」→ メイン
- 減色→「← 切り抜きへ戻る」、メイン→「← 切り抜きへ戻る」「← 減色へ戻る」
- マスク・色数・色差し替えは往復しても保持される（切り抜きをやり直して再度減色へ進んだ場合はパレット再計算、色差し替えはスロット番号で引き継ぎ）
- 確定時はマスク×255をアルファにしたRGBA ImageDataを生成し、透過PNGモードと同じ検出・出力フローに接続。ファイル名は `{元名}_cutout` ベース

## 3. オブジェクト検出

- **判定**: アルファ値 > アルファ閾値 のピクセルを前景とする
- **ラベリング**: 8近傍の反復flood fill（スタック方式・再帰なし）による連結成分ラベリング
- **ノイズ除去**: 面積 4px 未満の連結成分はオブジェクトとして扱わない（定数 `MIN_AREA = 4`）
- **選択**: 検出直後は全オブジェクトが選択状態。プレビュー上のクリックで選択／解除をトグル。バウンディングボックス内のクリックでも該当オブジェクトにヒットする
- 閾値・パディングの変更時は検出から再計算（スライダーは150msデバウンス）

## 4. 切り出しモード

| モード | 出力単位 | 内容 |
|---|---|---|
| 全体トリミング | 1ファイル | 選択中の全オブジェクトを含む最小矩形＋実効パディングで切り出し |
| 個別切り出し | オブジェクトごと | 各オブジェクトのバウンディングボックス＋実効パディングで切り出し。範囲内に重なる他オブジェクトのピクセルは透明化（背景=閾値以下のソフトなフリンジは保持） |

## 5. 出力タイプ

3タイプとも**同一キャンバスサイズ**で出力される（§7）。重ね合わせたときの位置ズレは発生しない。

### 5.1 カラーPNG
- 透明背景の切り出し画像。出力ピクセルのアルファ値は元画像のまま保持（閾値は検出判定のみに使用）
- ファイル名: `{元名}_trimmed.png`（全体） / `{元名}_obj{N}.png`（個別）

### 5.2 白版PNG（UVプリンター用白インキレイヤー）
- オブジェクトのシルエットを純白 `(255,255,255)` で塗ったPNG
- **収縮／拡張（-50〜+50px、既定0）**
  - プラス＝チョーク: 4近傍の収縮。印刷ズレ時の白のはみ出し防止
  - マイナス＝スプレッド: 4近傍の膨張。下地の白を輪郭より広く取る
  - 調整量0のときは元画像のアルファ値を白のアルファに引き継ぎ、エッジのアンチエイリアスを保持。調整時はベタ塗り（アルファ255）
- ファイル名: `{元名}_white.png` / `{元名}_obj{N}_white.png`

### 5.3 輪郭SVG（レーザーカッター用カットパス）
- マスク境界をトレースしたベクターSVG。赤線 `#ff0000`・`stroke-width:1`・`fill:none`・px単位
- 境界トレース方式: マスク境界の単位セグメントを列挙し閉ループに連結。穴（ドーナツ形状の内側輪郭）にも対応
- 頂点間引き: Douglas-Peucker（許容誤差1px）。閉ループの縮退（始点=終点）は最遠点分割で処理
- **オフセット（-50〜+50px、既定0）**
  - プラス＝外側: マスクを膨張させてからトレース（オブジェクトから離してカット）
  - マイナス＝内側: マスクを収縮させてからトレース（オブジェクトに食い込ませてカット）
- ファイル名: `{元名}_outline.svg` / `{元名}_obj{N}_outline.svg`
- 注意: px単位のため、レーザーカッターのソフトへの取り込み時は実寸へのスケールが必要

## 6. 調整パラメータ一覧

| パラメータ | 範囲 | 既定 | 作用 |
|---|---|---|---|
| アルファ閾値 | 0〜254 | 0 | この値以下のアルファのピクセルを背景扱い（ゴミ除去） |
| 余白パディング | 0〜500px | 0 | 切り出し範囲の周囲に付ける透明余白 |
| 白版の収縮／拡張 | -50〜+50px | 0 | ＋チョーク／−スプレッド（白版タイプ選択時に表示） |
| 輪郭のオフセット | -50〜+50px | 0 | ＋外側／−内側（輪郭タイプ選択時に表示） |

## 7. 実効パディング（キャンバスサイズの決定）

すべての出力タイプで共通の実効パディングを使用する:

```
実効パディング = 余白パディング + max(0, 白版スプレッド量, 輪郭の外側オフセット量)
```

- 白版のスプレッドや輪郭の外側オフセットで図形が広がる場合、その分だけ切り出し範囲が自動拡大され、はみ出しによる欠けが起きない
- 拡大は全タイプ共通のため、カラー・白版・輪郭のキャンバスサイズは常に一致する
- 出力キャンバスサイズ = `(バウンディングボックス幅+実効パディング×2) × (同 高さ+実効パディング×2)`

## 8. プレビュー

### 8.1 元画像（左）
- 市松模様背景＋元画像＋検出オーバーレイ（マスクエッジの着色、番号付きバウンディングボックス）
- 選択中はオブジェクトごとのパレット色・実線、非選択はグレー・破線
- クリックで選択トグル

### 8.2 切り出し結果（右・レイヤー合成）
- カラーPNG／白版PNG／輪郭SVGを**レイヤーとして重ねて表示**できる
- レイヤーパネル（上の行ほど前面）:
  - 表示ON/OFF（チェックボックス）
  - 不透明度 0〜100%（スライダー。白版の初期値50%）
  - ▲▼で順序入れ替え
- 初期状態はカラーのみ表示。出力タイプを選ぶと対応レイヤーが自動で表示ONになる
- 表示は中央揃え。キャプションに対象・寸法・合成中のレイヤー名を表示
- レイヤー合成はプレビュー確認用。ダウンロードは常にタイプごとの単体ファイル

## 9. 書き出し

- PNG: `canvas.toBlob("image/png")` → ダウンロード
- SVG: 文字列を `image/svg+xml` のBlobとして → ダウンロード
- 「すべてダウンロード」: 複数件を250ms間隔で順次ダウンロード発火

## 10. ファイル構成

| ファイル | 役割 |
|---|---|
| `outliner.html` | アプリ本体（HTML/CSS/JSすべて含む） |
| `index.html` | GitHub Pages用。`outliner.html` へ即時リダイレクト |
| `favicon.svg` | ファビコン（破線アウトラインのモチーフ） |
| `vendor/ort.wasm.min.js` | ONNX Runtime Web 1.14（写真モード用・162KB） |
| `vendor/ort-wasm-simd.wasm` | ort WASMバイナリ（写真モード用・9.5MB） |
| `vendor/u2netp.onnx` | セグメンテーションモデル（写真モード用・4.4MB） |
| `how-to-use.html` | 利用者向けの使い方ページ（アプリのヘッダーからリンク） |
| `ogp.png` | OGP画像（1200×630）。`ogp-source.html` から生成 |
| `ogp-source.html` | OGP画像の生成元HTML（ヘッドレスChromeでスクリーンショット） |
| `README.md` | 利用者向けの使い方 |
| `SPEC.md` | 本仕様書 |

## 11. 既知の制約

- 透過PNGモードの対応フォーマットはPNGのみ。写真モードは PNG・JPEG・WebP
- **写真モードは http(s):// 配信必須**（`file://` では vendor/ のfetchができない）
- 写真モードの作業解像度は長辺1000px（それ以上の原寸は縮小される）
- 複数ファイルの一括処理・ZIP出力は非対応（1枚ずつ処理する設計）
- 輪郭SVGの座標はピクセル境界ベース（サブピクセルの曲線近似はしない）
- 白版・輪郭の膨張／収縮は4近傍のモルフォロジー処理（オフセット形状は面取り付き＝厳密なユークリッド距離のラウンドオフセットではない）
- 全体トリミングのカラーPNGでは、範囲内に残る閾値以下の微小ピクセルはそのまま含まれる
